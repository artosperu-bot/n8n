import test from 'node:test';import assert from 'node:assert/strict';import { reduceState } from '../../src/conversation/state/StateReducer.ts';test('spin helper field is not leaked into canonical persisted state',()=>{const s=reduceState({spinFacts:[]},{budget:1500,spinResidual:'lo quiero para trabajo'});assert.deepEqual(s.spinFacts,['lo quiero para trabajo']);assert.equal(Object.prototype.hasOwnProperty.call(s,'spinResidual'),false);});
