# P0 concurrency hardening — status 2026-08-12

## Baseline protegido

- Workflow productivo: `STECH Ventas Consultivas`
- Workflow ID: `c661Gw0xoqZBsNtf`
- Baseline: `V45.68`
- Version ID: `dd01b10a-60e6-4412-903b-b21c7f3e577a`
- Cambios al workflow productivo durante esta fase: **NO**

## Estado confirmado

### Safe Renew — `public.ia_renovar_turno`

- Script A baseline/precheck: **PASS**.
- Script B ejecutado: creó `public.ia_renovar_turno(...)` sin reemplazar las RPC productivas protegidas.
- Lógica estructural verificada: inputs, lock, owner, lease vigente, queue exacta, queue owner, estado `PROCESSING` y `GREATEST()` para no acortar lease.
- Permisos finales: `EXECUTE` únicamente para `postgres` y `service_role`.
- Estado: **instalación + seguridad de acceso PASS**.
- Estado de comportamiento: **pendiente de 8 tests QA**.

### Safe Release — `public.ia_liberar_turno`

- Script C: **NO EJECUTADO**.
- Existe un defecto confirmado que puede permitir finalizaciones `DONE` indebidas, incluyendo `PENDING → DONE` en condiciones incorrectas.
- La corrección final no debe aplicarse antes de completar QA de Safe Renew y capturar/revisar DDL + rollback exacto.
- El overload antiguo sin `message_id` debe evaluarse con enfoque fail-closed (`MESSAGE_ID_REQUIRED`).
- Estado: **NO MODIFICAR TODAVÍA**.

### Persistencia — `public.ia_persistir_turno_atomico`

- Mantener sin cambios.
- El fencing existente valida lock, owner y lease vigente y debe preservarse.

### Renovación histórica — `fn_ia_renovar_session_lock`

- Sí valida que el lease no esté vencido, por lo que no resucita directamente un lease expirado.
- No es suficiente para P0 porque puede acortar un lease y no valida `message_id`, owner de queue ni estado `PROCESSING`.
- No conectarla a producción.

### Workflow QA

- Nombre: `STECH P0 Concurrency QA`
- ID: `nWdFJFw2gwiixKjm`
- Separado del workflow productivo.
- Uso: harness exclusivo para pruebas de concurrencia.

## Próximos tests de Safe Renew

1. `LOCK_NOT_FOUND`;
2. owner incorrecto;
3. `message_id` incorrecto/inexistente;
4. queue owner incorrecto;
5. queue con estado distinto de `PROCESSING`;
6. lease expirado;
7. renovación positiva;
8. comprobar que una renovación nunca acorta un lease más largo.

## Regla de promoción

No ejecutar Script C ni publicar cambios en V45.68 hasta que los 8 tests de Safe Renew pasen. Después: candidato Safe Release en QA → matriz P0 completa → regresión chatbot → recién entonces promoción/publicación.
